# 2026-08

## 下一步 CI 复验（2026-08-31）

- Windows `5fefbf8f` / run `33375209886` 与 Android `9d1837af` / run `33375405837` 均成功；Android Debug Artifact `9751779113` 已上传。
- API 只读检查确认双端 repository secrets 均为空，未进入正式发布或生产切换；真实设备验收仍待维护者下载候选包后执行。
- `release.yml` 统一为显式 `publish_release` 开关：默认只保留候选 Artifact，只有手动选中正式发布时才允许标签、GitHub Release 或 Windows `publish:*`。
- Windows 默认候选 Release run `33377401806` 验证成功并上传 x64 安装包 Artifact `9752534440`；Release 数量和最新标签均未变化，确认没有误触发正式发布。

## 2026-08-03 Windows 更新状态修复

- 修复自动更新触发、不可用更新器错误反馈和下载状态同步。
- 涉及 `autoUpdate.ts`、`useUpdate.ts`、`UpdateModal.vue`。
- 定向 lint、主进程构建和渲染进程构建通过；未执行真实安装器更新，因为用户要求不上传新版本。

## 2026-08-03 Windows 2.13.1 发布候选

- 版本从 `2.13.0` 提升到 `2.13.1`，包含更新下载状态修复。
- 构建 `VisonCube-Music-v2.13.1-x64-Setup.exe`、对应 `.blockmap` 和 `latest.yml`；包内版本、更新清单 SHA-512 与安装包一致。
- 安装包 SHA-256：`F33BE5B16263A2886FF518534C468DC5B1F1AA4ECE46877D44017366E8CE3E6C`。
- 旧版 `2.13.0` 已备份到 `D:\Code\VisonCube\exports`。COS 上传、服务器发布清单部署和真实客户端端到端更新仍待执行。

## 2026-08-03 Windows 2.13.1 线上发布

- 已在 COS `VisonCube/Music/Windows/` 发布安装包、`.blockmap` 和 `latest.yml`，保留了 `2.13.0` 历史对象。
- 已核验公网 `latest.yml` 为 `2.13.1`，安装包大小为 `108679883` 字节，支持 HTTP `206`；完整下载 SHA-256 与构建产物一致：`F33BE5B16263A2886FF518534C468DC5B1F1AA4ECE46877D44017366E8CE3E6C`。
- 生产发布清单仅更新 `music-windows`，原文件备份为 `/home/ubuntu/ServerCode/VisonCube/update/releases.json.bak-20260803T160421+0800`；`/api/releases/music-windows` 已返回 `2.13.1`、COS 下载地址和更新源地址。
- `visoncube-auth.service` 保持运行，但当前 SSH 账户无重启服务权限；真实旧客户端发现、下载、安装流程仍待设备验证。

## 2026-08-05 Windows 2.13.2 GitHub 源码发布

- 来源歌单同步、账号页状态、语言、模型测试和 `2.13.2` 版本源码已发布到 GitHub `main` 提交 `dcb854d8`。
- 远端父提交、tree SHA、提交消息和 `package.json` 版本已通过 GitHub API 回读；更新引用使用 `force=false`，未创建标签或 GitHub Release。
- 2026-08-31：Music 双端 ZIP 扁平部署到 `D:\Code\VisonCube\Music\Windows` 与 `D:\Code\VisonCube\Music\Android`；新增 Windows 关闭策略字段、统一关闭 IPC、首次关闭确认弹窗、托盘/退出设置和三语文案；移植双端稳定上游修复（版本关键词、IEC 容量、歌词毫秒、QQ 歌单总数/回退、Windows 列表顺序、Android MG 兼容与歌曲 URL 缓存清理）。因缺少 `D:\Code\VisonCube\依赖`，构建与真机验收未运行。
- 2026-08-31（补充）：修正双端 MG 歌曲 ID 与 Windows MG 歌词查询使用 `songmid`，同步毫秒格式化与逐字时间整数化；旧版本扁平 `tray.enable` 配置迁移时不再被嵌套字段覆盖。
- 2026-08-31（上游复核）：移植 2026-08-23 繁中窗口尺寸“更小”文案修复；语言排序和 Linux/ARM 构建映射提交未移植。
- 2026-08-31（依赖直链）：双端 9 个定制 npm 包从本地 `file:` 改为固定 GitHub Release `deps-2026-07-30-aeadf24`；依赖仓库通过 GitHub Actions 在云端拆包并发布 9 个 `.tgz` 与两份校验清单。Android Gradle Wrapper 改为官方 8.8 地址和固定 SHA-256；公开下载、资产 SHA-256、npm SHA-512 与锁文件已逐项回读验证，本机未下载依赖大包。
- 2026-08-31（云端 CI）：双端源码与依赖仓库均以普通非强制提交同步到 GitHub。Windows CI `33370107565` 验证 npm ci、测试、全量 ESLint 和完整构建；后续定向 lint 优化在 `33370642949` 再次全绿。Android CI `33369508808` 验证 npm ci、测试、Lint、JS bundle、Gradle Debug APK 和 Artifact；Android `.nvmrc` 固定 Node 22 以支持 TypeScript 模型测试。正式 Release 工作流均改为手动触发，未发布正式安装包/APK。
- 2026-08-31（候选包与服务器审查）：手动触发 Windows Beta `33373780718`，Windows/Win7/Linux/Mac 打包与 Artifact 上传全部成功；Android Debug Artifact `9749688825` 有效。公网 `music-windows`/`music-android` 更新接口和 COS 文件只读核对通过，未切换生产。Windows COS 上传工具改为环境变量凭据、产物白名单和只上传不删除；Android Release 增加签名 Secret 预检。真实 Windows/Android 设备验收仍待执行。

## 2026-08-31 首次启动页面切换修复

- Windows `useApp` 的历史页面恢复改为等待 `router.isReady()`，仅在默认首屏且路由未被用户改变时执行 `router.replace()`。
- 该修复针对“导航高亮已变化但主内容仍停留在搜索页”的启动竞争；待新候选包完成桌面回归后再关闭风险。
- 提交 `e43c7ab` 已普通推送至 Windows `main`；CI run `33387721505` 的测试、Lint 和完整源码构建全部成功。候选 Release 触发需 GitHub 登录授权，本轮未创建正式 Release 或生产产物。
- 候选 Release run `33388893148`（Artifact `9756799587`）成功；安装包 SHA-256 为 `E2C9382E18941640DFEA83B3DF0D0E4E1F6FC718E1B4B45A62E86C89C52A9445`，安装包与主程序均未签名。隔离桌面多页面切换冒烟通过，未创建正式 Release、未改版本号、未切换线上清单。

## 2026-08-31 2.13.4 签名与候选发布准备

- 核对 Android 远端加密签名备份与本地文件一致（SHA-256 `FE7990E78B4CA1A3DD4A585501F3819BB55BA67876F807FD99CE08D3A42E0B62`），公开 PEM 解析后的证书指纹为 `9C951C4BBA399D21751F4B194E839DA3A49EFD60534CF9B3B9D35859A6D6BC95`。
- 双端版本改为 `2.13.4`，Android `versionCode=85`；Release workflow 增加 Android `apksigner`、Windows Authenticode 和临时签名文件清理，候选阶段不创建标签或生产对象。
- 恢复口令、Android JKS、Windows PFX、GitHub Secrets 和真实设备验收尚未完成；正式 Release、COS 上传、服务器清单替换和生产推送保持门禁状态。
