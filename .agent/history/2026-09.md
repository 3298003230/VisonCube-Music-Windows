# 2026-09

## 2026-09-11 托管音源删除边界修复

- 按产品规则保留退出账号时的托管订阅源；仅用户手动删除托管源时清除双端独立缓存，避免重启后自动恢复。
- 普通本地/在线导入源的原生删除流程保持不变；新增 Windows/Android 接线回归测试，并将下一候选更新为双端 `2.13.6`、Android 基础 `versionCode=87`；不创建正式 Release或更新生产。

## 2026-09-11 托管音源账号切换收敛

- 发现单份缓存会记录最后更新账号：A 删除音源、B 更新、切回 A 后缓存归属不匹配，必须手动更新才能恢复。
- 统一改为退出账号即卸载账号订阅音源并清除缓存；若该音源正在使用则回退至可用内置音源。再次登录后必须手动更新音源或重新导入，普通导入源、歌单和同步数据不受影响。
- Android 将缓存/卸载职责收敛到既有 `features/musicSource` 下的 `storage.ts`；Windows 和 Android 接线测试分别通过 5/5、8/8。本轮未提交、推送、出包或更新生产。

## 2026-09-10 品牌与设置体验收敛

- 恢复双端 `V/C` 无封面占位、旧版六组设置分类和 Windows 首屏路由竞争保护；登录回首页与托管音源更新逻辑保持不变。
- 双端首次许可等待改为 5 秒；Android 启动器前景相对缩小 10%，Windows 应用图标相对放大 10%，托盘专用资源保持不变。
- 双端同步模型测试各通过 4/4，语言与图标资源解析、旧版实现对照及图标边界检查通过；未提交、推送、出包或更新生产。

## 2026-09-01 2.13.5 范围与构建链路收敛

- 确认保留 VisonCube Music 全部自定义功能，只补充原作者稳定修复和低风险冗余清理；不移动源码目录、不实现历史 TODO、不做大版本升级。
- 双端版本固定为 2.13.5，Android 基础 versionCode 为 86；Windows 继续未签名 x64 发布，Android 使用既有证书签名五个 APK。
- 双端发布元数据同步改为幂等操作，候选构建增加零警告 lint、版本、哈希、安装包清单与签名校验。
- 读取 Windows CI #20 的完整日志并最小修复 38 个未处理 Promise 与 5 个花括号错误；未改变 ESLint 规则、公共接口或目录结构。
- Windows 首个 2.13.5 候选发现发布元数据解析未兼容 CRLF；双端 `parseVersionSections` 现先统一换行，避免 Runner 行尾差异导致重复改写版本清单。
- Android 候选发现 `publish/index.js` 的 `no-void` 以及歌词请求包装的类型/async lint 错误，已按规则最小修复。
- 本地 TV 项目保持不变；未来仅允许在再次确认后清理精确列出的 COS 电视安装包对象。

## 2026-09-08 取消直连生产同步

- 按用户要求停止创建 COS 子用户密钥，不再通过 GitHub Actions 直连 Music COS 或服务器更新清单。
- 删除 Windows 仓库 GitHub Secret `VISONCUBE_SERVER_SSH_KEY`，撤销服务器上本轮专用 SSH 公钥并删除本机对应私钥/公钥文件。
- 从 Windows 源码与远端 `main` 删除 `.github/workflows/production-sync.yml`，保留双端 2.13.5 版本代码和既有候选/Release 构建链路。
- Windows `e20e19a` 推送后自动触发的 CI `34192474898` 已通过。

## 2026-09-08 Music 2.13.5 正式发布

- 用户测试候选包未发现问题后，正式发布 Windows/Android `v2.13.5` GitHub Release，并同步 Music COS 白名单资产。
- 通过腾讯云轻量应用服务器自动化助手备份并原子更新服务器 `releases.json`，只切换 `music-windows` 和 `music-android`。
- 发现更新接口使用 `asset_path` 派生下载文件名；补充同步 `asset_path` 后，公网接口、COS 文件长度、SHA-256、Windows Range 206、Android APK 指纹和包版本验证通过。

## 2026-09-09 原作者源码底座还原

- 以经验证的原作者最新双端快照重建 `src`，只保留 VisonCube 品牌、账号、云同步、托管音源、自有更新、Android 包身份/签名配置和 Windows 关闭策略。
- 清理用户可见界面中的无意义表情、原作者导向与旧品牌提示；许可证、第三方归属、同步兼容标识和评论内容解码保持不变。
- Windows 与 Android 正式 `src` 已逐文件 SHA-256 对照候选目录，双端来源歌单同步模型测试均通过 4/4；完整候选 CI 和真机验收仍是提交、推送及发布前门禁。

## 2026-09-10 源码同步与 CI 收敛

- 将双端还原后的源码普通提交并推送至 `main`；最终基线为 Windows `7822954`、Android `443c4ce`。
- Android 修复托管音源初始化返回值与异步调用契约后，CI #24 通过。
- Windows 修复关闭设置持久化接口，并恢复账号会话数据键、请求代理函数导出和 PNG 类型声明后，CI #38 通过。
- 双端正式 `src` 与临时 Git 工作区逐文件 SHA-256 一致；本轮未创建标签、Release、候选包，也未改动 COS 或服务器。

## 2026-09-10 对外展示与许可修复

- Windows 安装器三份许可文件恢复为 VisonCube Music 许可文本，修复候选安装包仍显示旧许可的问题。
- 双端公开 README、FAQ、Issue 模板、CHANGELOG 与发布更新元数据改为 VisonCube Music；保留顶层开源许可证、第三方归属和必要兼容标识。
- 未提交、推送、创建 Release、更新 COS 或服务器；Windows 需重新构建候选包验证安装器许可。

## 2026-09-10 候选构建元数据修复

- 对外展示提交已推送至 Windows `033cf5f` 与 Android `984e940`；候选 workflow 均使用 `publish_release=false`，正式发布任务被跳过。
- 首轮候选在发布元数据幂等性校验失败，原因是手写 `version.json` 与 `publish/changeLog.md` 的规范化输出不同；已使用项目 `publish` 脚本生成一致内容，待推送并重新构建。

## 2026-09-10 Android 托管音源播放修复

- 修复 Android `managed_user_api` 在播放适配器中被误判为内置音源的问题，并增加托管源播放路由回归测试；提交 `4189817` 已推送。
- 候选 run `34492760126` 全部通过，arm64-v8a APK 的 CI 哈希、包名、版本码、ABI 和既有签名证书均经本机复验；未创建 Release、未更新 COS 或服务器。

## 2026-09-11 更新下载安装回归修复

- 以 2.13.6 修复测试包验证升级起点，并将同一修复提升为双端 2.13.7、Android 基础 versionCode 89。
- 双端候选 Actions 完整通过；Windows 保持未签名，Android 五包继续使用既有证书并验证 Provider、包名、版本码与指纹。
- 本轮仅恢复更新下载/安装、关闭快捷键、双协议解析和单曲 URL 缓存清理接线，不改账号、同步、音源、播放、品牌、许可或依赖。
